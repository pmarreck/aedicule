# Dirtree: Stateful directory trees in the CLI for humans and LLMs. https://github.com/pmarreck/dirtree
ver=1.2
depth=3

annotate=[
	.cargo/config.toml = Nightly-only Wasm atomic shared-memory linker settings required by GPUI's browser backend
	.github/workflows/ci.yml = GitHub Actions gate for optimized Nix build plus the complete top-level test suite
	.mechatron-prime/targets = Mechatron Prime exact-SHA Nix package and pure test target manifest.
	ASSET_PACKAGE_PROPOSAL.md = Proposed bounded asset profile; blar owns lossless WAV/AIFF-to-FLAC preparation.
	Cargo.toml = Rust package metadata, GUI feature, pinned GPUI dependencies, and local tracing compatibility patches
	GPUI_REFRESH_API_PROPOSAL.md = Deferred human-owned upstream design for exact display refresh and presentation policy.
	MEMORIES = Project-specific durable lessons discovered during the spike
	MEMORIES/Archive code and published specification need a pinned compatibility profile = Archive-source/spec drift requires a narrow profile; blar owns lossless FLAC preparation.
	MEMORIES/Browser input probes must distinguish dispatch from guest delivery.frontmatter.md = Project lesson separating CDP dispatch, DOM receipt, GPUI callbacks, and WAT event delivery
	MEMORIES/Complete test suites should enter a pure flake environment = Durable lesson preventing global PATH tools from masking undeclared test dependencies.
	MEMORIES/Display refresh belongs in an explicit event, not the tick-rate selector = Records the display-refresh event and simulation-rate negotiation boundary.
	MEMORIES/Display-rate overrides must parse decimals lexically and aliases explicitly = Records exact decimal and canonical refresh-rate override parsing.
	MEMORIES/Fixed-step hosts should accumulate elapsed nanoseconds times hertz = Exact integer-rational host timing with bounded catch-up and no fractional-period drift
	MEMORIES/GPUI canvas elements need explicit size_full even inside a flex-filling parent = Project lesson from the missing-scene GPUI adapter defect and two-layer visual verification.
	MEMORIES/GPUI's simple image painter does not expose sprite-atlas source cropping = Adapter limitation and production-ready sprite rendering options
	MEMORIES/Guest-declared audio can remain application-agnostic and decimal-fixed through the PCM boundary = Durable design lesson for opaque guest synth programs, integer DSP, and one-way PCM conversion
	MEMORIES/Late fixed-step wakes need absolute deadlines and timestamped input = Absolute-deadline wake, timestamped event ordering, overload preservation, and hot-reload generation lesson
	MEMORIES/Launchers must resolve project paths from their own location = Records the adjacent-checkout launcher contract and fake-Nix regression strategy.
	MEMORIES/Live WAT reloads need transactional candidate swap and explicit state schema = Project lesson from content-watched transactional hot reload and real-window verification
	MEMORIES/Long headless simulations must divide Wasmtime work into refueled calls = Why deterministic headless simulation slices guest ticks across fresh Wasmtime fuel budgets
	MEMORIES/Mechatron needs a post-hook push to create its first badge = Records why a new Mechatron webhook needs a subsequent signed push before its dynamic badge exists.
	MEMORIES/Mutable WAT belongs outside the native Nix source closure = Why runtime application WAT must remain outside the native Rust derivation, with structural enforcement
	MEMORIES/Nix sandboxed CLI scripts need patched interpreter paths = Records preserving portable scripts while patching copied interpreters in Nix checks.
	MEMORIES/Pointer scroll should preserve line versus pixel units rather than invent a conversion.frontmatter.md = Why the WAT wheel ABI retains GPUI line versus precise-pixel units.
	PLAN.md = Active delivery plan: browser, runtime diagnostics, assets, and ABI evolution.
	PROJECT_OVERVIEW.md = Generic frontplane goals, architectural boundary, proof status, and terminology
	README.md = Project overview, usage, renderer validation contract, and live-reload diagnostics.
	RULES.md = Non-negotiable architecture, containment, determinism, and workflow constraints
	SPEC.md = Normative protocol, trust boundary, absolute-deadline scheduling, lifecycle, and feasibility specification
	WAT_ABI.md = Build-checked complete WAT ABI reference including input event payloads.
	WEB_DELIVERY.md = Browser bundle contract, serving instructions, payload budget, and headless startup diagnostics
	build = Top-level optimized Nix build and debug/test dispatcher
	build.rs = Fails builds when checked-in WAT ABI documentation drifts.
	docs/I18N.md = Prepare-phase localization boundary and canonical future locale policy
	flake.nix = Pure Nix native/test/web packages; web Cargo closure merges rust-src vendoring and pinned GPUI font assets.
	home/pmarreck/Code/ulam-flower-wat/PLAN.md = Small verified delivery plan for the radial flower demo.
	home/pmarreck/Code/ulam-flower-wat/README.md = Run instructions and WAT guest ownership for the demo.
	home/pmarreck/Code/ulam-flower-wat/code.wat = WAT guest: animated pointer-reactive radial flower.
	home/pmarreck/Code/ulam-flower-wat/test = Deterministic guest SVG render regression test.
	run = Generic optimized launcher for external WAT or the embedded fallback
	src/bin/aedicule-web.rs = GPUI Web adapter: animation-frame scheduler plus pointer and viewport forwarding.
	src/bin/generate-wat-abi.rs = Outputs the generated WAT ABI reference.
	src/bin/gpui-wasm-render.rs = Headless CLI that advances bundled or external WAT in fuel-safe slices and emits deterministic SVG
	src/fallback.wat = Stable embedded ABI-conformance fallback, independent of mutable application plugins.
	src/gpui_canvas.rs = Shared GPUI painter that replays validated command frames on native and browser frontplanes
	src/lib.rs = GPUI-independent WAT runtime, reload transaction, stable rejection diagnostics, and SVG core.
	src/main.rs = Native GPUI adapter with deterministic timing, transactional reload, input forwarding, synth audio, and stderr rejections.
	src/wat_abi.rs = Canonical generated WAT ABI declarations and client documentation text.
	src/wav.rs = Pure bounded RIFF/WAVE integer-PCM decoder yielding device-independent fixed-point clips.
	src/web.rs = Portable browser runtime controller for exact scheduled WAT frames and synthetic input ordering.
	test = Pure full suite for generic Rust, adapter, CLI, Nix partition, and repository-boundary checks
	tests/audio.rs = Generic host-side decimal-fixed oscillator, envelope, and filter structural regression
	tests/cli/build_partition = Nix graph regression proving default equals the native frontplane and excludes non-native inputs
	tests/cli/development_dependencies = Classifies the complete external executable set required by the pure test shell.
	tests/cli/launcher_cwd = Regression test that proves run resolves the flake and Cargo manifest from its own directory while preserving caller CWD.
	tests/cli/repository_boundary = Classifier preventing production application artifacts and behavior oracles from entering the generic repo
	tests/cli/web_delivery = Pure-Nix static browser bundle smoke test with raw Wasm size budget.
	tests/cli/web_startup_surface = Fast browser-delivery regression ensuring startup failures remain visible and capability-diagnostic.
	tests/composition.rs = Independent affine path, image, animated sprite, resource-budget, and stack-balance fixture
	tests/containment.rs = Hostile-WAT import, fuel, number, lifecycle, and budget containment checks
	tests/fixtures/animated.wat = Neutral stateful WAT fixture shared by generic SVG and render-CLI adapter tests
	tests/gui_cli.rs = Native GUI runner help/about/error CLI contract tests without opening a window
	tests/hot_reload.rs = Transactional candidate swap, compatible state transfer, schema restart, and rollback coverage
	tests/integer_profile.rs = Zero-float WAT ABI regression tests for integer lifecycle, controls, deterministic trig, and Q16 paths
	tests/integration/web_browser_startup = Headless Chromium/CDP probe for startup, canvas health, synthetic pointer/keyboard input, and identity-preserving multi-touch gestures
	tests/launch.rs = Launch resolution, file revision, fallback, and stable WAT rejection diagnostic tests.
	tests/lib/capture.bash = Project-local no-temp stdout/stderr/status capture helper for portable Bash tests
	tests/pointer_buttons.rs = Portable WAT proof for stable pointer buttons plus two-axis line/pixel scroll events.
	tests/render_cli.rs = CLI coverage for frame differences, spaced paths, help/about, and invalid options.
	tests/snapshot.rs = Neutral guest fixture proving generic render purity and snapshot restore/rejection semantics
	tests/svg_export.rs = Pure generic SVG artifact coverage with a neutral stateful WAT fixture
	tests/synth.rs = Guest-declared synth ABI, exact descriptor capture, and set-based invalid-field classifier tests
	tests/timing.rs = Deterministic fixed-step and live-adapter scheduling tests across every supported rate, event boundaries, jitter, and overload
	tests/wat_abi_doc.rs = Guards byte-exact generated WAT ABI documentation.
	tests/wav.rs = Independent fixed-byte WAVE fixtures for decoder validity, limits, and header rejection.
	third_party/ztracing = Apache-2.0 no-op compatibility surface replacing GPUI's unused GPL tracing path
	web/Caddyfile = Caddy development-server headers required by GPUI's shared-memory WebAssembly backend
	web/bootstrap.js = Loads code.wat before starting the generated Aedicule WebAssembly module
	web/cargo-config.toml = Scoped nightly Cargo flags required for GPUI's browser WebAssembly backend
	web/index.html = Minimal full-canvas HTML shell for a generic Aedicule web bundle
]
