[package]
name = "ztracing_macro"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Identity instrumentation attribute for the ztracing compatibility facade"

[lib]
proc-macro = true
