#!/usr/bin/env bash

set -u

case "${1:-}" in
	"")
		exec nix build --max-jobs 1 --cores 0
		;;
	--test)
		exec ./test
		;;
	--debug)
		exec nix develop -c cargo build
		;;
	*)
		printf 'usage: %s [--test|--debug]\n' "$0" >&2
		exit 2
		;;
esac
